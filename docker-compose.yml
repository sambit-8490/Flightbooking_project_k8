version: "3.8"

services:
   mongo:
     image: mongo:7
     container_name: mongo
     ports:
       - "27017:27017"
     volumes:
       - mongo_data:/data/db

   backend:
     build: ./backend
     container_name: backend
     ports:
       - "3000:3000"
     env_file:
       - ./backend/.env
     depends_on:
       - mongo

   frontend:
     build: ./frontend
     container_name: frontend
     ports:
       - "5173:5173"
     env_file:
       - ./frontend/.env
     depends_on:
       - backend

volumes:
   mongo_data:

